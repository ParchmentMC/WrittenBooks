plugins {
    id 'java-gradle-plugin'
    id 'org.parchmentmc.writtenbooks' version '0.2.0-dev-SNAPSHOT'
    id 'maven-publish'
}

project.group = 'org.parchmentmc'

repositories {
    jcenter()
}

dependencies {
    implementation 'org.eclipse.jgit:org.eclipse.jgit:+'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.7.1'
}

java.toolchain.languageVersion = JavaLanguageVersion.of(8)

test {
    useJUnitPlatform()
}

gradlePlugin {
    // Define the plugin
    plugins.register("writtenbooks") {
        id = 'org.parchmentmc.writtenbooks'
        implementationClass = 'org.parchmentmc.writtenbooks.WrittenBooksPlugin'
    }
}